name: database
description: A new Flutter package project.
version: 0.0.1
homepage:

publish_to: none

environment:
    sdk: ">=2.16.2 <3.0.0"
    flutter: ">=1.17.0"

dependencies:
    flutter:
        sdk: flutter
    
    core:
        path: ../core
    movie:
        path: ../movie
    tv_show:
        path: ../tv_show

    sqflite_sqlcipher: ^2.1.0
    encrypt: ^5.0.1

dev_dependencies:
    flutter_test:
        sdk: flutter

    flutter_lints: ^1.0.0

flutter:
